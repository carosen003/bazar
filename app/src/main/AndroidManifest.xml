<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light">
        <activity
            android:name=".ui.activities.VisorImagen"
            android:exported="false"
            android:label="@string/title_activity_visor_imagen"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity
            android:name=".ui.activities.inicioActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.CrearCuentaActivity" />
        <activity android:name=".ui.activities.MenuPrincipalActivity" />
        <activity android:name=".ui.activities.LoginActivity" />
        <activity android:name=".ui.activities.VenderActivity" />
        <activity android:name=".ui.activities.verMisProductos" />
        <activity android:name=".ui.activities.AcercaDeActivity" />
    </application>

</manifest>